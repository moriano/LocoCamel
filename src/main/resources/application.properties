# Common configuration shared across all profiles

# Server configuration
server.port=8080

# Application name
spring.application.name=loco-camel

# Active profile (can be overridden via environment variable or command line)
# Use 'dev' for local development, 'prod' for production
spring.profiles.active=dev
